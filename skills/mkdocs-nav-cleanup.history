# mkdocs-nav-cleanup — History

## v1.2.0 (2026-05-25)

**Changed by:** Scorched-earth doc/repro/workflow purge PR (~32 file deletions) where mkdocs `--strict` doc-deploy failed 3 consecutive times because broken cross-references to deleted files were only discovered post-hoc when CI ran.
**Verification:** verified-ci

### What changed

- Added new "Pre-Deletion Audit" subsection under Verified Workflow with a copy-paste bash snippet that grep's surviving docs and `mkdocs.yml` for references to to-be-deleted files BEFORE the deletion commit is pushed.
- Reframed the skill so the pre-deletion audit is the preferred path; the post-hoc nav cleanup from v1.1.0 remains as the fallback when a deletion has already shipped.
- Added 3 new Failed Attempts rows documenting the three CI iteration failures from the scorched-earth deletion PR (each iteration caught only one batch of broken links because no exhaustive audit was run upfront).
- Added a note about doc-inventory enumerations (e.g. `"ADR-001 through ADR-XYZ"` in `docs/README.md`) that are not link-checkable by mkdocs but ARE catchable by basename grep — they should be included in the audit.
- Updated `description` and `When to Use` to surface the pre-deletion audit trigger.

### Why

v1.1.0 was a reactive skill: it told you how to clean up `mkdocs.yml` and broken cross-links AFTER `mkdocs build --strict` had already failed in CI. In practice this means each cleanup round only catches what that specific CI run surfaced — if the surviving docs reference multiple deleted files, each push fixes one batch and the next push re-fails on the next batch. The scorched-earth PR demonstrated this with 3 separate red CI runs.

The pre-deletion audit reduces this to a single push: build a basename pattern from the deletion list, grep the entire surviving doc tree plus `mkdocs.yml`, fix every hit in the same commit. Empirically this catches all iterations in one pass (verified by running the same audit grep post-hoc and getting a clean result after the third fix landed).

### Previous version (v1.1.0) snapshot

```markdown
---
name: mkdocs-nav-cleanup
description: 'Fix MkDocs build failures from nav referencing deleted files or relative
  links escaping the docs/ tree. Use when: (1) CI ''Deploy Documentation'' job fails
  after deleting doc stubs, (2) mkdocs build --strict reports missing files, (3) PR
  deletes placeholder docs but mkdocs.yml nav still references them, (4) --strict
  rejects a relative link like ../../docs/dev/file.md that escapes the docs/ root.'
category: ci-cd
date: 2026-03-27
version: 1.1.0
user-invocable: false
---

## Overview

| Field | Value |
| ------- | ------- |
| **Skill** | mkdocs-nav-cleanup |
| **Category** | ci-cd |
| **Trigger** | CI Deploy Documentation failure after deleting placeholder doc stubs |
| **Scope** | mkdocs.yml nav section + any cross-links in remaining docs |

## When to Use

- A PR deletes placeholder/stub documentation files and CI "Deploy Documentation" fails
- `mkdocs build --strict` reports "Documentation file 'X.md' specified in nav is not found"
- A remaining markdown file contains a relative link to a now-deleted file
- Pre-existing link-check failures (root-relative paths) conflated with PR-caused failures
- A relative link uses `../../docs/dev/` from inside `docs/adr/` — escaping the mkdocs
  `docs/` root under `--strict`

## Verified Workflow

1. **Identify deleted files** — check the PR diff for removed `.md` files
2. **Audit mkdocs.yml nav** — find all nav entries referencing the deleted files
3. **Audit remaining docs for cross-links** — grep for relative links pointing to deleted files:

   `grep -r "deleted-file-name" docs/ mkdocs.yml`

4. **Remove nav entries** from `mkdocs.yml` — delete the entries (and their parent section
   if now empty)
5. **Remove broken relative links** from remaining docs — remove or update bullet points
   referencing deleted files
6. **Verify no remaining references**:

   `grep -r "deleted-file1\|deleted-file2" docs/ mkdocs.yml`

7. **Run pre-commit on changed files**:

   `pre-commit run --files mkdocs.yml docs/path/to/changed.md`

8. **Commit** with message format `fix: Address review feedback for PR #<N>`

## Failed Attempts

| Attempt | What Was Tried | Why It Failed | Lesson Learned |
| --------- | ---------------- | --------------- | ---------------- |
| Removing entire section blocks at once | Used Edit to replace multi-line nav blocks covering Core + Advanced + Development | Initially included Release Process (a surviving file) in the removal | Always check which files in a nav section still exist before removing the whole block |
| Treating link-check failures as in-scope | Considered fixing 20 root-relative path errors in CLAUDE.md, docs/adr/, notebooks/README.md | These were pre-existing on main, unrelated to the PR | Verify pre-existing CI failures with `gh run list --branch main --workflow "Check Markdown Links"` before scoping fixes |

(Results & Parameters and Relative Link Escaping sections preserved unchanged — see git history at v1.1.0 for full text.)
```

---

## v1.1.0 (2026-03-27)

**Initial tracked version.** Added "Relative Link Escaping the docs/ Root" guidance for `--strict` rejecting `../../docs/...` paths.
