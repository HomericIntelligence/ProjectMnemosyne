# Raw Notes: Skills-to-Plugins Migration (2026-02-22)

## Session Context

**Date**: 2026-02-22
**Project**: ProjectMnemosyne
**Trigger**: 10 PRs in ProjectMnemosyne added skills to flat `skills/` directory. CI only triggers on `plugins/**` paths. Branch protection requires the `validate` status check, so these PRs could never merge.

**Prior session** had already completed: PRs #131 (duplicate), #137, #138 (stacked into #139), #139 (3 skills), #152.

**This session** completed: PRs #155, #158, #160, #165, #167.

## PRs Migrated

### PR #155 — `gitattributes-setup`

- **Branch**: `skill/vcs/gitattributes-setup`
- **Original location**: `skills/gitattributes-setup/`
- **New location**: `plugins/tooling/gitattributes-setup/`
- **Issues fixed**:
  - Category `vcs` → `tooling`
  - Added YAML frontmatter to SKILL.md
  - Added Overview table (from existing header table)
  - Failed Attempts converted from prose to pipe table
  - Restored root `.claude-plugin/plugin.json` to main's version (branch had clobbered it)

### PR #158 — `unit-tests-untested-modules`

- **Branch**: `skill/testing/unit-tests-untested-modules`
- **Original location**: `skills/testing/unit-tests-untested-modules/`
- **New location**: `plugins/testing/unit-tests-untested-modules/`
- **Issues fixed**:
  - Added YAML frontmatter to SKILL.md
  - Added Overview table (from existing property table)
  - Failed Attempts converted from prose subsections to pipe table
  - Rewrote plugin.json: removed non-standard fields (`trigger_conditions`, `outcomes`, `related_skills`, `applies_to`, `author`, `created`)
  - Preserved `references/notes.md`

### PR #160 — `doc-stale-future-improvements-audit`

- **Branch**: `skill/documentation/doc-stale-future-improvements-audit`
- **Original location**: `skills/doc-stale-future-improvements-audit/`
- **New location**: `plugins/documentation/doc-stale-future-improvements-audit/`
- **Issues fixed**:
  - Added YAML frontmatter to SKILL.md
  - Failed Attempts converted from prose subsections to pipe table
  - Created plugin.json (only SKILL.md existed, no plugin.json on this branch)

### PR #165 — `pixi-lock-rebase-regenerate`

- **Branch**: `skill/ci-cd/pixi-lock-rebase-regenerate`
- **Original location**: `skills/pixi-lock-rebase-regenerate/`
- **New location**: `plugins/ci-cd/pixi-lock-rebase-regenerate/`
- **Issues fixed**:
  - Added YAML frontmatter to SKILL.md
  - Added Overview table (from existing header table)
  - Renamed `## Failed Approaches` → `## Failed Attempts`
  - Failed Attempts converted from prose subsections to pipe table
  - Created plugin.json (only SKILL.md existed)

### PR #167 — `issue-triage-and-fix`

- **Branch**: `skill/evaluation/issue-triage-and-fix`
- **Original location**: `skills/issue-triage-and-fix/`
- **New location**: `plugins/evaluation/issue-triage-and-fix/`
- **Issues fixed**:
  - Added YAML frontmatter to SKILL.md
  - Rewrote plugin.json: removed non-standard fields (`skill_name`, `created_date`, `use_cases`, `prerequisites`, `success_metrics`, `workflow_summary`, `key_commands`, `lessons_learned`, `applicable_to`, `not_applicable_to`)
  - Preserved `references/session-notes.md`

## Common Issues Found Across All PRs

1. **Missing YAML frontmatter** — every SKILL.md lacked the `---` block
2. **Missing Overview table** — some had the data but not under `## Overview`
3. **Failed Attempts as prose** — subsections (`### ❌ Attempt 1:`) not recognized as pipe tables
4. **Non-standard plugin.json fields** — especially when generated by LLMs that add schema-like fields
5. **Invalid category** — `vcs` not in approved list

## Validation Command

```bash
python3 scripts/validate_plugins.py plugins/
# Must show: ALL VALIDATIONS PASSED
```

## Final State

All 7 PRs from the two sessions (#139, #152, #155, #158, #160, #165, #167) merged successfully via auto-merge after force-push triggered CI.
